export module interfaceUser {
    export interface detail{
        id?: number;
        nickname: string;
        level: number;
        password: string;
        create_date: string;
        delele_date?: string;
    }
}